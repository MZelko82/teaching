# Normalise all text files to LF in the repo; convert on checkout per-OS
* text=auto eol=lf

# Explicit text files — always LF
*.py    text eol=lf
*.json  text eol=lf
*.svg   text eol=lf
*.tex   text eol=lf
*.md    text eol=lf
*.txt   text eol=lf

# Binary files — never touch line endings
*.mp4   binary
*.png   binary
*.jpg   binary
*.jpeg  binary
*.pdf   binary
