# ── Python ───────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]

# ── Virtual environments ──────────────────────────────────────────────────────
.venv/
venv/
env/

# ── Manim intermediate output (regenerated by `manim-slides render`) ──────────
#
#   media/videos/  — raw animation clips before manim-slides packages them
#   media/Tex/     — LaTeX → SVG compilation cache (speeds up re-renders,
#                    but can be fully regenerated; ~100s of small files)
#   media/images/  — exported still frames
#   media/texts/   — text SVG cache
#
#   NOTE: slides/ is intentionally NOT ignored — it contains the packaged
#   MP4s and PValue.json that `manim-slides present` needs to run.
#
**/media/videos/*
!/media/videos/pvalue_slides/1080p60/PValue.mp4
**/media/Tex/
**/media/images/
**/media/texts/

# ── Claude Code local settings ────────────────────────────────────────────────
.claude/
